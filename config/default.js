const config = {
    //启动端口
    port: 8080,
    HOST: 'http://localhost:',
    //数据库配置
    database: {
        DATABASE: 'nodesql',
        USERNAME: 'root',
        PASSWORD: 'jannina@@',
        PORT: '3306',
        HOST: 'localhost'
    }
}

module.exports = config;  